const tempSearchItemsFunction = (itemArray) => {
    itemArray.forEach((element) => {
        if(element[9] != 0) {
            console.log(element)
        }
    });
}

// tempSearchItemsFunction(ItemOBJ)
